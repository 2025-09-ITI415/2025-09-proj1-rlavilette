using UnityEngine;

public class StopperButton : MonoBehaviour
{
    private LRbar lrBar;
    public float lrBarspeed;
 

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void stopButton()
    {
        lrBarspeed = 0f; 
    }
}
